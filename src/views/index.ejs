<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Post</title>
    <link rel="stylesheet" href="/style.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-4bw+/aepP/YC94hEpVNVgiZdgIC5+VKNBQNGCHeKRQN+PtmoHDEXuppvnDJzQIu9" crossorigin="anonymous">
</head>
<body>
    <div class="d-grid gap-2 col-6 mx-auto">
        <h1 class="container text-center">POST</h1>
        <a class="btn btn-primary" href="/notes/create">Create new Post</a>
        <br>
      </div>
    <ul>
        <% allNotes.forEach((note) => { %>
            <li>
                <div class="container text-center w-50 h-50 ">                                    
                    <div class="card mb-3">
                        <img src="<%= note.image %>" class="card-img-top" alt="">
                        <div class="card-body">
                          <h5 class="card-title"><%= note.title %></h5>
                          <p class="card-text"><%= note.content %></p>
                          <p class="card-text"><small class="text-body-secondary"><%= note.author %></small></p>
                          <p class="card-text"><small class="text-body-secondary"><%= note.createdAt %></small></p>
                        </div>
                        
                     
                      </div>
                      

                    <a type="button" class="btn btn-outline-warning" href="/notes/update/<%= note.id %>">Update</a>
                    <a type="button" class="btn btn-outline-danger" href="/notes/delete/<%= note.id %>">Delete</a>
                </div>
            </li>
        <% }); %>
    </ul>
</body>
</html>